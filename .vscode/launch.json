{
  "version": "0.2.0",
  "configurations": [{
    "type": "node",
    "request": "launch",
    "name": "Launch Program",
    "program": "${workspaceFolder}/src/main.ts",
    "outFiles": ["${workspaceFolder}/dist/main.js"],
    "skipFiles": [
      "<node_internals>/**"
    ]
  }]
}